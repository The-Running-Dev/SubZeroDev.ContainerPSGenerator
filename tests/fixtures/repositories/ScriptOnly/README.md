# Script Fixture

This maintained fixture represents a repository that exposes PowerShell scripts but
does not yet contain a container module specification.

```text
docker run --rm ghcr.io/example/script-fixture:latest
```
